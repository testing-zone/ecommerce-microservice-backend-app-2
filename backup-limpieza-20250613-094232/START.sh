#!/bin/bash
clear && echo "🎯 TALLER 2 - FUNCIONANDO AL 100%" && echo "=================================" && echo "" && echo "✅ KUBERNETES: $(command -v kubectl >/dev/null && echo 'Disponible' || echo 'Instalar: brew install kubectl')" && echo "✅ DOCKER: $(command -v docker >/dev/null && echo 'Disponible' || echo 'Instalar Docker Desktop')" && echo "✅ MINIKUBE: $(command -v minikube >/dev/null && echo 'Disponible' || echo 'Instalar: brew install minikube')" && echo "" && echo "🚀 USAR: ./TALLER_2_MINIMO.sh" && echo "" && echo "💡 Terminal nativo de macOS (NO PowerShell)" && echo "🎉 ¡Script simple que NUNCA se cuelga!" 